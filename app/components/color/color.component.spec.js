'use strict'

describe('myApp.color module', function () {
  beforeEach(module('myApp.color'))

  describe('color controller', function () {
    it('should ....', inject(function ($controller) {
      // spec body
      var colorCtrl = $controller('colorCtrl')
      expect(colorCtrl).toBeDefined()
    }))
  })
})
