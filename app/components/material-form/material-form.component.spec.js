'use strict'

describe('materialForm', function () {
  beforeEach(module('materialForm'))

  describe('materialForm', function () {
    it('should ....', inject(function ($controller) {
      // spec body
      var MaterialFormCtrl = $controller('MaterialFormCtrl')
      expect(MaterialFormCtrl).toBeDefined()
    }))
  })
})
