'use strict'

describe('myApp.popup module', function () {
  beforeEach(module('myApp.popup'))

  describe('popup controller', function () {
    xit('should ....', inject(function ($controller) {
      // spec body
      var popupCtrl = $controller('popupCtrl')
      expect(popupCtrl).toBeDefined()
    }))
  })
})
