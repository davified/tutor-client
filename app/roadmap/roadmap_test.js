'use strict';

describe('myApp.roadmap module', function() {

  beforeEach(module('myApp.roadmap'));

  describe('roadmap controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var roadmapCtrl = $controller('roadmapCtrl');
      expect(roadmapCtrl).toBeDefined();
    }));

  });
});