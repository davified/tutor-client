'use strict';

describe('myApp.exercise module', function() {

  beforeEach(module('myApp.exercise'));

  describe('exercise controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var exerciseCtrl = $controller('exerciseCtrl');
      expect(exerciseCtrl).toBeDefined();
    }));

  });
});
